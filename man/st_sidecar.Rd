% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_registry.R
\name{st_sidecar}
\alias{st_sidecar}
\alias{.st_write_sidecar}
\alias{.st_read_sidecar}
\title{Write sidecar metadata (internal)}
\usage{
.st_write_sidecar(path, meta)

.st_read_sidecar(path)
}
\arguments{
\item{path}{Character path of the data file whose sidecar will be
read.}

\item{meta}{List or other object convertible to JSON via
\code{jsonlite::write_json()}.}
}
\value{
Invisibly returns \code{NULL}.

A list (parsed JSON) when the sidecar exists, otherwise
\code{NULL}.
}
\description{
Write \code{meta} (a list) as JSON to the sidecar file for \code{path}. The
JSON is first written to a temporary file in the same directory and
then moved into place to reduce risk of partial writes.

Read the sidecar metadata for \code{path} if it exists, returning
\code{NULL} when no sidecar file is present. Preference order is JSON
first, then QS2. When a QS2 variant is encountered the function
will attempt to use \code{qs2} then \code{qs}.
}
\keyword{internal}
