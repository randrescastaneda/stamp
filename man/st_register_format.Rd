% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_registry.R
\name{st_register_format}
\alias{st_register_format}
\title{Register or override a format handler}
\usage{
st_register_format(name, read, write, extensions = NULL)
}
\arguments{
\item{name}{Character scalar: format name (e.g. \code{"qs2"}, \code{"rds"}).}

\item{read}{Function \verb{function(path, ...)} returning an R object.}

\item{write}{Function \verb{function(object, path, ...)} that writes \code{object} to \code{path}.}
}
\value{
Invisibly returns \code{TRUE} on success.
}
\description{
Public function that allows users to register a new format handler
or override an existing one. Handlers must be functions with the
expected signatures documented below.
}
\examples{
st_register_format(
  "txt",
  read  = function(p, ...) readLines(p, ...),
  write = function(x, p, ...) writeLines(x, p, ...)
)
}
