% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_pk.R
\name{st_pk}
\alias{st_pk}
\title{Normalize a primary-key specification}
\usage{
st_pk(x = NULL, keys, validate = TRUE, check_unique = FALSE)
}
\arguments{
\item{x}{Optional data.frame to validate the keys against. If \code{NULL}, only
the \code{keys} vector is normalized.}

\item{keys}{Character vector of column names comprising the primary key.}

\item{validate}{Logical; when \code{TRUE} validate that columns exist (and
uniqueness if \code{check_unique} is \code{TRUE}).}

\item{check_unique}{Logical; when \code{TRUE} assert that \code{keys} uniquely
identify rows in \code{x} (only checked when \code{x} is provided).}
}
\value{
A list with element \code{keys} containing the canonical character vector.
}
\description{
Normalize and validate a primary-key (pk) specification. The canonical
representation is a list with element \code{keys} containing a character vector
of column names. When \code{x} (a data.frame) is provided and \code{validate = TRUE}
the function will check that the columns exist (and optionally that they
uniquely identify rows).
}
