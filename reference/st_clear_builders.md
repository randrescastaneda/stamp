# Clear all builders (or only those for a given path)

Clear all builders (or only those for a given path)

## Usage

``` r
st_clear_builders(path = NULL)
```

## Arguments

- path:

  Optional character path; if provided, only builders for that path are
  removed.
