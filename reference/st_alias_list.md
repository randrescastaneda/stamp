# List registered stamp aliases in the current session

List registered stamp aliases in the current session

## Usage

``` r
st_alias_list()
```

## Value

data.frame with columns: alias, root, state_dir, stamp_path
